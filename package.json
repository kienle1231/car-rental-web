{
  "name": "car-rental-monorepo",
  "version": "1.0.0",
  "scripts": {
    "install-all": "cd frontend && npm install && cd ../backend && npm install",
    "build": "cd frontend && npm install && npm run build && cd .. && mkdir -p backend/public && cp -r frontend/dist/* backend/public/",
    "start": "node index.js"
  },
  "engines": {
    "node": "22.x"
  }
}
